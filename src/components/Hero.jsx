import {motion} from "framer-motion";

export const Hero = () => {
  return <motion.section id = "home" className="hero" initial={{opacity:0}}>
    <div className="hero-container">

    </div>
  </motion.section>
};
